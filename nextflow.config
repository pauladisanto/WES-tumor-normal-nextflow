process {
  executor = 'local'
  errorStrategy = 'terminate'
  shell = ['/bin/bash', '-euo', 'pipefail']
}

profiles {
  conda {
    conda.enabled = true
    process.conda = "${projectDir}/wes-nextflow-environment.yml"
  }
}
